generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  // Runtime queries go through Supabase's transaction pooler (port 6543,
  // pgbouncer=true). IPv4-reachable from Railway and connection-efficient.
  url       = env("DATABASE_URL")
  // Migrations / `prisma db push` need a non-pooled (session) connection,
  // since the transaction pooler can't run schema DDL. Use the session
  // pooler (port 5432 on ...pooler.supabase.com), also IPv4-reachable.
  directUrl = env("DIRECT_URL")
}

model User {
  id            String        @id @default(cuid())
  name          String?
  email         String?       @unique
  password      String?
  walletAddress String?       @unique
  createdAt     DateTime      @default(now())
  bankAccount   BankAccount?
  transfers     BankTransfer[]
}

model BankAccount {
  id            String   @id @default(cuid())
  userId        String   @unique
  bankName         String
  accountNumber    String
  accountHolder    String
  recipientAddress String   @default("")
  createdAt        DateTime @default(now())
  updatedAt     DateTime @updatedAt
  user          User     @relation(fields: [userId], references: [id])
}

model BankTransfer {
  id           String    @id @default(cuid())
  userId       String
  amountMYR    Float
  status       String    @default("PENDING")
  referenceNo  String    @unique
  bankName     String
  accountLast4 String
  createdAt    DateTime  @default(now())
  completedAt  DateTime?
  user         User      @relation(fields: [userId], references: [id])
}

model LoanTransaction {
  id          String   @id @default(cuid())
  wallet      String
  type        String
  amount      String
  txHash      String   @unique
  blockNumber Int
  createdAt   DateTime @default(now())

  @@index([wallet])
}

model KycSubmission {
  id          Int      @id @default(autoincrement())
  wallet      String   @unique
  fullName    String
  icNumber    String
  dob         String
  gender      String
  nationality String
  phone       String
  email       String
  addr1       String
  addr2       String   @default("")
  postcode    String
  city        String
  state       String
  employment  String
  income      String
  purpose     String
  fundSource  String
  icFrontPath  String   @default("")
  icBackPath   String   @default("")
  selfiePath   String   @default("")
  status      String   @default("pending")
  submittedAt DateTime @default(now())
  updatedAt   DateTime @updatedAt
}
