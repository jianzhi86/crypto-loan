import ExplorerContent from './ExplorerContent';

export const metadata = {
  title: 'Transaction Explorer — CryptoLend',
  description: 'Public feed of CryptoLoan protocol activity: deposits, borrows, repayments and withdrawals.',
};

export default function ExplorerPage() {
  return <ExplorerContent />;
}
